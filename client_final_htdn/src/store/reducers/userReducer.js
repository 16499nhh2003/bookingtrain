const initState =  {
    userData :  {}
}

const userReducer =  (state =  initState , action) => { 
    switch (action.key) {    
        default:
            return state;
    }
}

export default userReducer;